info:
  name: post users
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/users
  body:
    type: json
    data: |-
      {
        "username": "hyunjin",
        "email": "hyunjin1612@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
